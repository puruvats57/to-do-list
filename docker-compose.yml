version: '3'
services:
  server:
    build:
      context: ./server/
    image: akkyou/server
    command: "node server.js"
    
    ports:
      - "5000:5000"
    links:
      - mongo
    
  client:
    build:
      context: ./client/
    image: akkyou/client
    command: "npm start"
    
    
    ports:
      - "3000:3000"
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
